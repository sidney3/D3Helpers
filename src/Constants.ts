
const DEFAULT_COLOR: string = "rgb(255, 255, 255)"; // Default to white
const DEFAULT_STROKE_WIDTH: number = 1;
const DEFAULT_BORDER_COLOR: string = "rbg(0, 0, 0)"; // Default to black background

export {DEFAULT_COLOR, DEFAULT_STROKE_WIDTH, DEFAULT_BORDER_COLOR}